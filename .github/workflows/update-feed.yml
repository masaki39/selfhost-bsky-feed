name: Update Bluesky Feed

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  run-feed:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript
        run: npm run build

      - name: Run feed generator
        run: npm run start
        env:
          BSKY_APP_HANDLE: ${{ secrets.BSKY_APP_HANDLE }}
          BSKY_APP_PASSWORD: ${{ secrets.BSKY_APP_PASSWORD }}
          BSKY_SERVICE: ${{ vars.BSKY_SERVICE || secrets.BSKY_SERVICE }}
          BSKY_SEARCH_QUERY: ${{ vars.BSKY_SEARCH_QUERY || secrets.BSKY_SEARCH_QUERY }}
          BSKY_SEARCH_LANG: ${{ vars.BSKY_SEARCH_LANG || secrets.BSKY_SEARCH_LANG }}
          BSKY_MUTE_WORDS: ${{ vars.BSKY_MUTE_WORDS || secrets.BSKY_MUTE_WORDS }}

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./data

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
